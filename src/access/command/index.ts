// Command layer — sentinel self-management (write side of Access zone).
// Commands control this sentinel only — never reach through to observed systems.
// Candidates: addMachine, removeMachine, triggerCollection, pauseCollection, reloadConfig.
